name: Python Pipelone
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@latest
    - run: python hello.py
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@latest
    - uses: actions/upload-artifact@latest
      with:
        name: hello
        path: .
